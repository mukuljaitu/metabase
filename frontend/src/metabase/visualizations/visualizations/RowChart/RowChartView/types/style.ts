export type ChartTheme = {
  axis: {
    color: string;
    ticksFontSize: number;
    ticksFontWeight: number;
  };
  dataLabels: {
    color: string;
    fontSize: number;
    fontWeight: number;
  };
  goal: {
    color: string;
    fontSize: number;
    fontWeight: number;
  };
  grid: {
    color: string;
  };
};
